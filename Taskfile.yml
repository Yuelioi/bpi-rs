version: '3'

vars:
  CARGO_TEST_ARGS: ":: -- --test-threads=1 --nocapture"

tasks:
  check:
    cmds:
      - cargo check

  fmt_check:
    cmds:
      - cargo fmt --check

  test_compile:
    desc: Compile all library tests without running legacy live tests.
    cmds:
      - cargo test --all-features --lib --no-run

  test_offline:
    desc: Run default offline library tests.
    cmds:
      - cargo test --all-features --lib

  check_all:
    desc: Safe default checks for the migration baseline.
    cmds:
      - cargo fmt --check
      - cargo check --all-features
      - cargo test --all-features --lib

  lint_strict:
    desc: Run strict clippy; expected to fail until legacy migration debt is cleaned up.
    cmds:
      - cargo clippy --all-targets --all-features --locked -- -D warnings

  test_live_legacy:
    desc: Run ignored legacy live tests. Treat this as potentially mutating until each test is classified.
    preconditions:
      - sh: test "$BPI_LIVE_TEST" = "1"
        msg: Set BPI_LIVE_TEST=1 before running live Bilibili API tests.
      - sh: test "$BPI_MUTATING_TEST" = "1"
        msg: Set BPI_MUTATING_TEST=1 because legacy live tests are not yet classified as read-only.
    cmds:
      - cargo test --all-features --lib -- --ignored --test-threads=1 --nocapture

  test_live_mutating:
    desc: Alias for running ignored legacy live tests that may mutate account state.
    preconditions:
      - sh: test "$BPI_LIVE_TEST" = "1"
        msg: Set BPI_LIVE_TEST=1 before running live Bilibili API tests.
      - sh: test "$BPI_MUTATING_TEST" = "1"
        msg: Set BPI_MUTATING_TEST=1 before running mutating Bilibili API tests.
    cmds:
      - task: test_live_legacy

  test_activity:
    cmds:
      - cargo test --features activity activity{{.CARGO_TEST_ARGS}}

  test_article:
    cmds:
      - cargo test --features article article{{.CARGO_TEST_ARGS}}

  test_audio:
    cmds:
      - cargo test --features audio audio{{.CARGO_TEST_ARGS}}

  test_bangumi:
    cmds:
      - cargo test --features bangumi bangumi{{.CARGO_TEST_ARGS}}

  test_cheese:
    cmds:
      - cargo test --features cheese cheese{{.CARGO_TEST_ARGS}}

  test_clientinfo:
    cmds:
      - cargo test --features clientinfo clientinfo{{.CARGO_TEST_ARGS}}

  test_comment:
    cmds:
      - cargo test --features comment comment{{.CARGO_TEST_ARGS}}

  test_creativecenter:
    cmds:
      - cargo test --features creativecenter creativecenter{{.CARGO_TEST_ARGS}}

  test_danmaku:
    cmds:
      - cargo test --features danmaku danmaku{{.CARGO_TEST_ARGS}}

  test_dynamic:
    cmds:
      - cargo test --features dynamic dynamic{{.CARGO_TEST_ARGS}}

  test_electric:
    cmds:
      - cargo test --features electric electric{{.CARGO_TEST_ARGS}}

  test_fav:
    cmds:
      - cargo test --features fav fav{{.CARGO_TEST_ARGS}}

  test_historytoview:
    cmds:
      - cargo test --features historytoview historytoview{{.CARGO_TEST_ARGS}}

  test_live:
    cmds:
      - cargo test --features live live{{.CARGO_TEST_ARGS}}

  test_login:
    cmds:
      - cargo test --features login login{{.CARGO_TEST_ARGS}}

  test_manga:
    cmds:
      - cargo test --features manga manga{{.CARGO_TEST_ARGS}}

  test_message:
    cmds:
      - cargo test --features message message{{.CARGO_TEST_ARGS}}

  test_misc:
    cmds:
      - cargo test --features misc misc{{.CARGO_TEST_ARGS}}

  test_note:
    cmds:
      - cargo test --features note note{{.CARGO_TEST_ARGS}}

  test_opus:
    cmds:
      - cargo test --features opus opus{{.CARGO_TEST_ARGS}}

  test_search:
    cmds:
      - cargo test --features search search{{.CARGO_TEST_ARGS}}

  test_user:
    cmds:
      - cargo test --features user user{{.CARGO_TEST_ARGS}}

  test_video:
    cmds:
      - cargo test --features video video{{.CARGO_TEST_ARGS}}

  test_video_ranking:
    cmds:
      - cargo test --features video_ranking video_ranking{{.CARGO_TEST_ARGS}}

  test_vip:
    cmds:
      - cargo test --features vip vip{{.CARGO_TEST_ARGS}}

  test_wallet:
    cmds:
      - cargo test --features wallet wallet{{.CARGO_TEST_ARGS}}

  test_web_widget:
    cmds:
      - cargo test --features web_widget web_widget{{.CARGO_TEST_ARGS}}
