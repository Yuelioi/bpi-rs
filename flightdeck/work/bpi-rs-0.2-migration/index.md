# Index — bpi-rs 0.2 migration

## State

Design approved at conversation level: bpi-rs should become a high-quality, idiomatic Rust SDK, with breaking changes allowed. The public API should move toward module clients such as `client.video().info(...)`, not a flat 300+ method surface.

This topic owns the staged migration plan and should keep all active design and planning artifacts under this folder.

The current migration execution protocol is `api-upgrade-protocol.md`. Agents must follow module-batch migration, real Probe evidence, local-only raw Probe output under `target/...`, and low commit count rules from that protocol.

The accepted promoted contract shape is now one endpoint contract plus response fixtures:

```text
tests/contracts/<domain>/<endpoint>/contract.json
tests/contracts/<domain>/<endpoint>/responses/<case>.json
```

Do not resume the old committed shape `tests/contracts/<domain>/<endpoint>/<profile>.request.json` for promoted endpoint contracts.

`migration-status.md` is a local temporary status board for context recovery. It tracks pinned execution order, current batch, module status, evidence, and next action. It must not be committed. This workspace excludes it through `.git/info/exclude`; if another workspace needs the board, create the local file again instead of adding it to version control.

## Next

- Use `goal.md`, `api-upgrade-protocol.md`, and `migration-status.md` to choose the next module batch.
- Default to a real endpoint contract batch with Probe evidence unless a non-Probe shared-core/domain-client bridge is explicitly selected and recorded as such.
- The latest endpoint-candidate audit found no new safe read batch to promote. `manga/download-read` is marked not implemented for the current migration; do not keep default continuation blocked on it. Before running more Probe work, identify either a newly implemented/read-safe endpoint not covered by existing contracts, a user-approved dedicated proof-provider/API-design batch for manga reader proof fields, or an explicitly enabled gated/mutating batch.
- The latest module-client coverage audit found no new safe non-Probe bridge batch among promoted contracts. `login.qr.flow` is the only promoted contract name not directly present in `src/**/client.rs`, and it is a Probe flow contract composed from existing `LoginClient::qr_generate` and `LoginClient::qr_poll` endpoint methods.
- The remaining flat API compatibility decision has been made: old direct `BpiClient` flat async methods were removed in `flat-api/remove-remaining-legacy-flat-methods`. Current direct public async inventory under `impl BpiClient` is `COUNT=0`.
- The old write/session/mutating capability surface has been restored on module clients in `48cd13c feat(api): move legacy write surface to module clients`, preserving old method names and signatures on domain clients while keeping direct `BpiClient::...` flat methods removed.
- For each batch, follow `api-upgrade-protocol.md`.
- Update `migration-status.md` after each batch, but do not commit it.

## Read now

- goal.md
- api-upgrade-protocol.md
- plans/batch-todolist.md
- plans/module-client-coverage-audit.md
- plans/legacy-flat-api-surface-audit.md
- plans/gated-flat-api-release-policy.md
- docs/migration-0.2.md
- examples/module_clients.rs
- migration-status.md
- design.md
- plan.md
- flightdeck/knowledge/rust/sdk-quality.md

## Read if

- plans/ — when implementation planning begins.
- plans/manga-download-read-probe-block.md — before retrying manga download/read contracts.
- plans/remaining-endpoint-contract-audit.md — before claiming another safe Probe read batch remains or before selecting non-Probe bridge work.

## Progress

Done:
- Created active Flightdeck topic package.
- Wrote the 0.2 migration design spec.
- Installed and reviewed Rust best-practices, async-patterns, and rust-testing skills as reference material for future sessions.
- Researched primary Rust/Tokio/reqwest/tracing docs and distilled the SDK quality bar into `knowledge/rust/sdk-quality.md`.
- Added logging/observability and SDK embedding customization requirements to `design.md` and `knowledge/rust/sdk-quality.md`.
- Added type, fixture, and documentation discipline requirements to address weak models, random test parameters, and stale examples.
- Read all root `src/*.rs` files and sampled key submodules for errors, signing, login, video, dynamic, user, tests, README, Cargo, and Taskfile.
- Wrote `plan.md` with code-research findings and a staged migration plan.
- Wrote `api-upgrade-protocol.md` with Probe-first module-batch migration rules.
- Wrote local-only `migration-status.md` for pinned execution order and resumable batch state.
- Completed pilot `clientinfo/ip` request-contract validation in commit `c261e4f`.
- Migrated existing pilot contracts to endpoint `contract.json` plus `responses/*.json` fixtures in commit `6383119`.
- Covered `clientinfo/ip`, `login/vip-info`, `login/read-info`, and `login/qr` in the accepted contract shape.
- Completed `electric/private-read-remark-detail-flow`: anonymous direct Probe preserves `requires_login`, normal/vip Probe flows extract a private `remark_id` from `remark-list` at runtime, and promoted contracts/fixtures avoid committing literal private ids or message content.
- Rechecked `manga/download-read` with additional normal/vip `ComicDetail`, `epId`, alternate public samples, and user-provided current reader pages. Old-shape SDK Probe trials still return HTTP 200/API `code = 99`, but real-browser CDP and Playwright capture of `mc28218/469901` and VIP-owned `mc28218/469956` succeeds when the frontend supplies reader proof fields: `GetImageIndex` uses `ep_id + m2`, and `ImageToken` uses `urls + m1`. Playwright also mapped the proof path to current reader Go-WASM modules (`a1_o8iso5`, `y1_z2w2a3`, `a1_h17mj9`). The feature is now marked not implemented in the current SDK migration; `src/manga/download.rs` keeps models only, and further work requires a dedicated proof-provider/API-design batch. Details are in `plans/manga-download-read-probe-block.md`.
- Audited remaining endpoint candidates against source request calls, promoted `tests/contracts/**/contract.json`, and `migration-status.md`; no new safe Probe read batch was found. Details are in `plans/remaining-endpoint-contract-audit.md`.
- Completed explicit non-Probe `web_widget/module-client-bridge`: `BpiClient::web_widget()` now exposes a `WebWidgetClient` with payload-returning `region_banner`, `header_page`, and `online` methods backed by already promoted `web_widget` contracts. No Probe run or contract promotion was expected for this bridge batch.
- Completed explicit non-Probe `activity/module-client-bridge`: `BpiClient::activity()` now exposes an `ActivityClient` with payload-returning `info`, `list`, and `list_default` methods backed by already promoted `activity` contracts. No Probe run or contract promotion was expected for this bridge batch.
- Completed explicit non-Probe `audio/module-client-bridge`: `BpiClient::audio()` now exposes an `AudioClient` with payload-returning `info`, `tags`, `members`, `lyric`, `status_number`, `collection_status`, `coin_count`, `stream_url_web`, `stream_url`, `collections_list`, `collection_info`, `hot_menu`, `rank_menu`, `rank_period`, `rank_detail`, and `rank_music_list` methods backed by already promoted audio read contracts. `collection_info` returns an optional payload because promoted success fixtures record `data: null`, rank methods preserve the existing empty-CSRF fallback, and mutating/action endpoints remain excluded. No Probe run or contract promotion was expected for this bridge batch.
- Completed explicit non-Probe `article/module-client-bridge`: `BpiClient::article()` now exposes an `ArticleClient` with payload-returning `info`, `view`, `cards`, and `articles` methods backed by already promoted article read contracts. WBI-signed `view` and `cards` reuse the existing client signing helper; mutating `article/action` endpoints remain excluded. No Probe run or contract promotion was expected for this bridge batch.
- Completed explicit non-Probe `fav/module-client-bridge`: `BpiClient::fav()` now exposes a `FavClient` with payload-returning `folder_info`, `created_list`, `collected_list`, `resource_infos`, `list_detail`, and `resource_ids` methods backed by already promoted fav read contracts. Mutating `fav/action.rs` endpoints remain excluded. No Probe run or contract promotion was expected for this bridge batch.
- Completed explicit non-Probe `cheese/module-client-bridge`: `BpiClient::cheese()` now exposes a `CheeseClient` with payload-returning `info`, `info_by_season_id`, `info_by_ep_id`, `ep_list`, and `video_stream` methods backed by already promoted cheese info, ep-list, and playurl contracts. No Probe run or contract promotion was expected for this bridge batch.
- Completed explicit non-Probe `bangumi/module-client-bridge`: `BpiClient::bangumi()` now exposes a `BangumiClient` with payload-returning `info`, `detail`, `detail_by_season_id`, `detail_by_ep_id`, `sections`, `timeline`, and `video_stream` methods backed by already promoted bangumi info, timeline, and playurl contracts. Mutating `bangumi/follow.rs` endpoints remain excluded. No Probe run or contract promotion was expected for this bridge batch.
- Completed explicit non-Probe `video_ranking/module-client-bridge`: `BpiClient::video_ranking()` now exposes a `VideoRankingClient` with payload-returning `popular_list`, `popular_series_list`, `popular_series_one`, `popular_precious`, `ranking_list`, `region_dynamic`, `region_tag_dynamic`, `region_newlist`, and `region_newlist_rank` methods backed by already promoted video_ranking read contracts. `popular_series_one` reuses the existing WBI signing helper. No Probe run or contract promotion was expected for this bridge batch.
- Completed explicit non-Probe `search/module-client-bridge`: `BpiClient::search()` now exposes a `SearchClient` with payload-returning `article`, `bangumi`, `bili_user`, `live`, `live_room`, `live_user`, `movie`, `video`, `default`, `suggest`, and `hotwords` methods backed by already promoted search read contracts. Typed search and default search reuse the existing WBI signing helper; `hotwords` preserves the raw JSON response shape. No Probe run or contract promotion was expected for this bridge batch.
- Completed explicit non-Probe `wallet/module-client-bridge`: `BpiClient::wallet()` now exposes a `WalletClient` with payload-returning `info` over the already promoted authenticated wallet read contract. No Probe run or contract promotion was expected for this bridge batch.
- Completed explicit non-Probe `opus/module-client-bridge`: `BpiClient::opus()` now exposes an `OpusClient` with payload-returning `space_feed` over the already promoted public opus space-feed contract. No Probe run or contract promotion was expected for this bridge batch.
- Completed explicit non-Probe `misc/module-client-bridge`: `BpiClient::misc()` now exposes a `MiscClient` with payload-returning `buvid3`, `buvid`, `b23_short_link`, `bili_ticket`, and `bili_ticket_string` methods backed by already promoted misc contracts. No Probe run or contract promotion was expected for this bridge batch.
- Completed explicit non-Probe `message/module-client-bridge`: `BpiClient::message()` now exposes a `MessageClient` with payload-returning `unread_count`, `reply_feed`, and `single_unread` methods backed by already promoted message read contracts. Mutating `message_send` remains excluded. No Probe run or contract promotion was expected for this bridge batch.
- Completed explicit non-Probe `manga/module-client-bridge`: `BpiClient::manga()` now exposes a `MangaClient` with payload-returning `season_info`, `clock_in_info`, `user_point`, `point_products`, and `coupons` methods backed by already promoted manga read-core contracts. `download-read` remains Probe-blocked, and mutating buy/clock-in/share/exchange endpoints remain excluded. No Probe run or contract promotion was expected for this bridge batch.
- Completed explicit non-Probe `electric/module-client-bridge`: `BpiClient::electric()` now exposes an `ElectricClient` with payload-returning `month_up_list`, `video_show`, `recharge_list`, `rank_recent`, `charge_record`, `upower_item_detail`, `charge_follow_info`, `upower_member_rank`, `remark_list`, and `remark_detail` methods backed by already promoted electric public/private read contracts. Mutating B coin, message send, and remark reply endpoints remain excluded; `remark_detail` requires a caller-provided remark id while promoted flow contracts prove how valid private ids can be discovered. No Probe run or contract promotion was expected for this bridge batch.
- Completed explicit non-Probe `creativecenter/module-client-bridge`: `BpiClient::creativecenter()` now exposes a `CreativeCenterClient` with payload-returning season, videos, statistics, and railgun read methods backed by already promoted creativecenter contracts. Mutating season edit/action, upload, and opus deletion endpoints remain excluded. No Probe run or contract promotion was expected for this bridge batch.
- Completed explicit non-Probe `dynamic/module-client-bridge`: `BpiClient::dynamic()` now exposes a `DynamicClient` with payload-returning feed, detail, content, and lottery notice read methods backed by already promoted dynamic contracts. Deprecated legacy card/basic_info endpoints and mutating action/publish endpoints remain excluded. No Probe run or contract promotion was expected for this bridge batch.
- Completed explicit non-Probe `vip/module-client-bridge`: `BpiClient::vip()` now exposes a `VipClient` with payload-returning `center_info` over the already promoted VIP center-info read contract. Mutating VIP action endpoints and disabled clock-in remain excluded. No Probe run or contract promotion was expected for this bridge batch.
- Completed explicit non-Probe `comment/module-client-bridge`: `BpiClient::comment()` now exposes a `CommentClient` with payload-returning `list`, `replies`, `hot`, and `count` methods backed by already promoted comment read contracts. Mutating `comment/action` endpoints remain excluded, and `hot` returns an optional payload because promoted success fixtures record `data: null`. No Probe run or contract promotion was expected for this bridge batch.
- Completed explicit non-Probe `historytoview/module-client-bridge`: `BpiClient::historytoview()` now exposes a `HistoryToViewClient` with payload-returning `history_list`, `history_shadow`, and `toview_list` methods backed by already promoted history/to-view read contracts. Mutating history delete/clear/shadow-set and to-view add/delete/clear endpoints remain excluded. No Probe run or contract promotion was expected for this bridge batch.
- Completed explicit non-Probe `note/module-client-bridge`: `BpiClient::note()` now exposes a `NoteClient` with payload-returning `is_forbid`, `private_info`, `public_info`, `archive_list`, `user_private_list`, `public_archive_list`, and `user_public_list` methods backed by already promoted note read contracts. Mutating `note/action` add/delete endpoints remain excluded. No Probe run or contract promotion was expected for this bridge batch.
- Completed explicit non-Probe `danmaku/module-client-bridge`: `BpiClient::danmaku()` now exposes a `DanmakuClient` with payload/optional-payload JSON read methods plus raw bytes/parsed XML read methods backed by already promoted danmaku contracts. Mutating send/recall/thumbup/report/edit/buy endpoints remain excluded. No Probe run or contract promotion was expected for this bridge batch.
- Completed explicit non-Probe `live/public-core-client-bridge`: `BpiClient::live()` now exposes a `LiveClient` with payload-returning `area_list`, `room_info`, `stream`, `recommend`, and `version` methods backed by already promoted live public-core contracts. Mutating create/update/start/stop/send/report/moderation endpoints and other live sub-batches remain excluded. No Probe run or contract promotion was expected for this bridge batch.
- Completed explicit non-Probe `login/safe-flow-client-bridge`: `BpiClient::login()` now exposes payload-returning safe read/flow methods for read-info, daily reward, VIP info, login notice/log, captcha generation, and QR generate/poll over already promoted login contracts. SMS/password/cookie refresh/logout/sign update paths remain excluded. No Probe run or contract promotion was expected for this bridge batch.
- Completed explicit non-Probe `user/module-client-bridge`: `BpiClient::user()` now exposes a `UserClient` with payload-helper-backed public/relation read methods over already promoted user public-read and relation-read contracts. Relation group/action mutations remain excluded. No Probe run or contract promotion was expected for this bridge batch.
- Completed explicit non-Probe `video/module-client-bridge`: existing `BpiClient::video()` / `VideoClient` read methods now return decoded payloads directly through request payload helpers for `view`, `detail`, `page_list`, `desc`, and signed `play_url`, reusing already promoted video info-read and playurl contracts. Mutating action/report/collection-action endpoints remain excluded. No Probe run or contract promotion was expected for this bridge batch.
- Wrote `plans/batch-todolist.md` to shift continuation into larger reviewable batches: record explicit non-Probe work before code, keep TDD inside the batch, run focused/full verification once, update deck/status once, and commit once per completed batch.
- Completed explicit non-Probe `video/collection-player-client-bridge`: `VideoClient` now exposes payload-helper-backed collection read methods (`seasons_archives_list`, `home_seasons_series`, `seasons_series_list`, `series_info`, `series_archives`) and player read methods (`online_total`, `player_info_v2`, `related_videos`, `homepage_recommendations`, `ai_summary`, `tags`, `interactive_video_info`) over already promoted video collection-read and player-read contracts. Mutating action/report/collection-action endpoints remain excluded. No Probe run or contract promotion was expected for this bridge batch.
- Updated `goal.md` so goal-mode continuation treats `plans/batch-todolist.md` as the execution entry point alongside `api-upgrade-protocol.md`: select or add a reviewable batch first, record it in local status, run TDD/focused/full verification inside the batch, update deck/status once, and commit once per completed batch.
- Completed explicit non-Probe `live/remaining-read-client-bridge`: `LiveClient` now exposes payload-helper-backed methods over already promoted live gift-read, room-interaction-read, account-private-read, guard-read, moderation-private-read, and telemetry-read contracts (`gift_types`, `room_gift_list`, `blind_gift_info`, `danmu_info`, `emoticons`, `lottery_info`, `my_medals`, `follow_up_list`, `follow_up_web_list`, `replay_list`, `guard_list`, `silent_users`, `banned_users`, `shield_keywords`, and `web_heart_beat`). Mutating live create/update/start/stop/send/report/moderation-write endpoints remain excluded. No Probe run or contract promotion was expected for this bridge batch.
- Completed non-Probe docs/code `shared-core/module-client-coverage-audit`: compared 206 promoted `tests/contracts/**/contract.json` names against `src/**/client.rs` labels and found no missing safe bridge batch. The only unmatched promoted contract name is `login.qr.flow`, which is a Probe flow contract composed from `login.qr_generate` and `login.qr_poll`, both already exposed through `LoginClient` and covered by tests. Details are in `plans/module-client-coverage-audit.md`.
- Completed non-Probe docs `docs/readme-module-api-refresh`: rewrote `README.md` from the old 0.1 flat API narrative to the current 0.2 module-client direction. The README now shows `BpiClient::builder()`, explicit cookie/account setup, module-client payload-returning examples, QR login primitives through `LoginClient`, custom request helpers, offline default checks, and local-only Probe credential guidance.
- Completed non-Probe docs `docs/migration-guide-0.2`: added `docs/migration-0.2.md` with call-site migration guidance for independent client construction, explicit credentials, flat methods to module clients, payload-returning `BpiResult<T>`, error helpers, QR login primitives, custom requests, Probe/local credential rules, and a migration checklist. README now links to this guide.
- Completed non-Probe example/docs `examples/module-client-quickstart`: added `examples/module_clients.rs` as a compile-checked module-client quickstart that stays offline unless `BPI_RUN_EXAMPLE=1`; README links to the example and documents `cargo check --all-features --examples`.
- Completed non-Probe release cleanup `examples/stale-binary-quickstart-refresh`: refreshed `src/main.rs` from old flat `bangumi_info(...)` plus envelope-style handling to the module-client `client.bangumi().info(...)` quickstart, defaulting offline unless `BPI_RUN_EXAMPLE=1`.
- Completed non-Probe release verification `release/rustdoc-example-audit`: ran `cargo test --doc` as the Stage 6 rustdoc example gate; it passed with 0 doc tests, so no source-doc edits were needed.
- Completed non-Probe release verification `release/current-stack-full-gate`: ran the project full gate over the accumulated docs/example/source cleanup stack; `task check_all`, examples, bins, rustdoc, diff checks, stale API searches, and ignored-artifact checks passed.
- Completed non-Probe release cleanup audit `shared-core/legacy-flat-api-surface-audit`: counted 328 public async methods still declared directly on `impl BpiClient`, aggregated them by module, conservatively split 227 read/helper-like methods from 101 mutating/gated/flow-sensitive candidates, and recorded follow-up removal/deprecation batch boundaries in `plans/legacy-flat-api-surface-audit.md`.
- Completed non-Probe release cleanup source batch `flat-api/remove-small-read-shims`: removed six low-risk flat `BpiClient` read shims after their module-client replacements were already available (`clientinfo_ip`, `wallet_info`, `opus_space_feed`, `web_widget_region_banner`, `web_widget_header_page`, and `web_widget_online`), and rewrote the corresponding ignored live tests to call module clients.
- Completed non-Probe release cleanup source batch `flat-api/remove-activity-message-read-shims`: removed six more low-risk flat read shims after module-client replacements were already available (`activity_info`, `activity_list`, `activity_list_default`, `message_unread_count`, `message_reply_feed`, and `message_single_unread`), and rewrote the corresponding ignored live tests to call `client.activity()` and `client.message()`. `message_send` remains excluded as a mutating endpoint.
- Completed non-Probe release cleanup source batch `flat-api/remove-bangumi-cheese-read-shims`: removed 14 flat read/playurl shims after module-client replacements were already available (`bangumi_info`, `bangumi_detail`, `bangumi_detail_by_season_id`, `bangumi_detail_by_epid`, `bangumi_sections_by_season_id`, `bangumi_timeline`, `bangumi_video_stream`, `bangumi_video_stream_by_epid`, `bangumi_video_stream_by_cid`, `cheese_info`, `cheese_info_by_season_id`, `cheese_info_by_ep_id`, `cheese_ep_list`, and `cheese_video_stream`). Tests now use `client.bangumi()`, `client.cheese()`, or direct typed-param validation. `bangumi_follow` and `bangumi_unfollow` remain excluded as mutating endpoints.
- Completed non-Probe release cleanup source batch `flat-api/remove-search-video-ranking-read-shims`: removed 20 flat read shims after module-client replacements were already available (`search_article`, `search_bangumi`, `search_bili_user`, `search_live`, `search_live_room`, `search_live_user`, `search_movie`, `search_video`, `search_default`, `search_suggest`, `search_hotwords`, `video_popular_list`, `video_popular_series_list`, `video_popular_series_one`, `video_popular_precious`, `video_ranking_list`, `video_region_dynamic`, `video_region_tag_dynamic`, `video_region_newlist`, and `video_region_newlist_rank`). Ignored live tests now use `client.search()` and `client.video_ranking()`.
- Completed non-Probe release cleanup source batch `flat-api/remove-fav-note-history-comment-read-shims`: removed 20 flat read shims after module-client replacements were already available (`fav_folder_info`, `fav_created_list`, `fav_collected_list`, `fav_resource_infos`, `fav_list_detail`, `fav_resource_ids`, `note_is_forbid`, `note_get_private_info`, `note_get_public_info`, `note_list_archive`, `note_list_user_private`, `note_list_public_archive`, `note_list_public_user`, `history_list`, `history_shadow_get`, `toview_list`, `comment_list`, `comment_replies`, `comment_hot`, and `comment_count`). Ignored live tests now use `client.fav()`, `client.note()`, `client.historytoview()`, and `client.comment()`. Mutating favorite/note/history/to-view/comment actions remain excluded.
- Completed non-Probe release cleanup source batch `flat-api/remove-misc-vip-read-shims`: removed six flat read/helper shims after module-client replacements were already available (`misc_buvid3`, `misc_buvid`, `misc_b23_short_link`, `misc_sign_bili_ticket`, `misc_sign_bili_ticket_string`, and `vip_center_info`). Ignored live tests now use `client.misc()` and `client.vip()`. `vip_sign` and VIP action methods remain excluded.
- Completed non-Probe release cleanup source batch `flat-api/remove-article-manga-read-shims`: removed nine flat read shims after module-client replacements were already available (`article_info`, `article_view`, `article_cards`, `article_articles_info`, `manga_season_info`, `manga_clock_in_info`, `manga_user_point`, `manga_point_products`, and `manga_coupons`). Ignored live tests now use `client.article()` and `client.manga()`. Article actions, manga buy/clock-in/share/exchange mutations, and Probe-blocked `manga/download-read` remain excluded.
- Completed non-Probe release cleanup source batch `flat-api/remove-electric-read-shims`: removed ten flat electric read shims after module-client replacements were already available (`electric_month_up_list`, `electric_video_show`, `electric_recharge_list`, `electric_rank_recent`, `electric_charge_record`, `electric_upower_item_detail`, `electric_charge_follow_info`, `electric_upower_member_rank`, `electric_remark_list`, and `electric_remark_detail`). Ignored live tests now use `client.electric()`. B coin, message send, and remark reply mutations remain excluded.
- Completed non-Probe release cleanup source batch `flat-api/remove-audio-read-shims`: removed 16 flat audio read shims after module-client replacements were already available (`audio_info`, `audio_tags`, `audio_members`, `audio_lyric`, `audio_status_number`, `audio_collection_status`, `audio_coin_count`, `audio_stream_url_web`, `audio_stream_url`, `audio_collections_list`, `audio_collection_info`, `audio_hot_menu`, `audio_rank_menu`, `audio_rank_period`, `audio_rank_detail`, and `audio_rank_music_list`). Ignored live tests now use `client.audio()`. Collection mutation, coin mutation, and rank subscription methods remain excluded.
- Completed non-Probe release cleanup source batch `flat-api/remove-video-read-shims`: removed 17 flat video read shims after module-client replacements were already available (`video_info`, `video_detail`, `video_pagelist`, `video_desc`, `video_playurl`, `video_seasons_list`, `video_series_list`, `video_seasons_series_list`, `video_series_info`, `video_series_archives`, `video_online_total`, `video_player_info_v2`, `video_related_videos`, `video_homepage_recommendations`, `video_ai_summary`, `video_tags`, and `video_interactive_video_info`). Ignored live tests now use `client.video()`. Video action/report/collection-action mutations and static/helper modules remain excluded.
- Completed non-Probe release cleanup source batch `flat-api/remove-creativecenter-read-shims`: removed 14 flat creativecenter read shims after module-client replacements were already available (`season_list`, `season_info`, `season_by_aid`, `season_section_episodes`, `up_archives_list`, `up_archive_videos`, `up_stat`, `up_archive_compare`, `up_article_stat`, `up_video_trend`, `up_article_trend`, `up_play_source`, `up_viewer_data`, and `up_electromagnetic_info`). Ignored live tests now use `client.creativecenter()`. Upload, opus delete, season action, and season edit mutations remain excluded.
- Completed non-Probe release cleanup source batch `flat-api/remove-danmaku-read-shims`: removed 12 flat danmaku read/binary/XML shims after module-client replacements were already available (`danmaku_history_dates`, `danmaku_snapshot`, `danmaku_thumbup_stats`, `danmaku_adv_state`, `danmaku_web_seg_proto`, `danmaku_web_seg_wbi_proto`, `danmaku_web_view_proto`, `danmaku_mobile_seg_proto`, `danmaku_web_history_seg_proto`, `danmaku_history_xml_bytes`, `danmaku_xml_list_so`, and `danmaku_xml_list`). Ignored live tests now use `client.danmaku()`. Send, recall, buy, thumbup action, report, and edit mutations remain excluded.
- Completed non-Probe release cleanup source batch `flat-api/remove-dynamic-read-shims`: removed 13 flat dynamic read shims after module-client replacements were already available (`dynamic_all`, `dynamic_check_new`, `dynamic_nav_feed`, `dynamic_feed_banner`, `dynamic_detail`, `dynamic_reactions`, `dynamic_lottery_notice`, `dynamic_forwards`, `dynamic_pics`, `dynamic_forward_item`, `dynamic_live_users`, `dynamic_up_users`, and `dynamic_recent_up_list`). Ignored live tests now use `client.dynamic()`. Deprecated legacy 404/basic_info APIs and action/publish mutations remain excluded.
- Completed non-Probe release cleanup source batch `flat-api/remove-user-read-shims`: removed 18 flat user public/relation read shims after module-client replacements were already available (`user_relation_stat`, `user_up_stat`, `user_navnum`, `user_album_count`, `user_space_info`, `user_card_info`, `user_card_info_with_photo`, `user_card_info_without_photo`, `user_cards`, `user_infos`, `user_space_notice`, `user_bangumi_follow_list`, `user_contributed_videos`, `user_medal_wall`, `user_name_to_uid`, `user_followings`, `user_followers`, and `user_follow_tags`). Ignored live tests now use `client.user()`. Relation group/action mutations and space notice mutation remain excluded.
- Completed non-Probe release cleanup source batch `flat-api/remove-live-read-shims`: removed 20 flat live read/telemetry shims after module-client replacements were already available (`live_area_list`, `live_room_info`, `live_stream`, `live_recommend`, `live_version`, `live_gift_types`, `live_room_gift_list`, `live_blind_gift_info`, `live_get_danmu_info`, `live_emoticons`, `live_lottery_info`, `live_my_medals`, `live_follow_up_list`, `live_follow_up_web_list`, `live_replay_list`, `live_guard_list`, `live_list_silent_users`, `live_list_banned_users`, `live_list_shield_keyword`, and `live_web_heart_beat`). Ignored live tests now use `client.live()`. Room management, send danmu, and moderation mutations remain excluded.
- Completed non-Probe release cleanup source batch `flat-api/remove-login-safe-read-flow-shims`: removed 15 flat login safe read/flow shims and helpers after `LoginClient` replacements were already available (`login_info_user_stat`, `login_info_nav_info`, `login_info_user_info`, `login_info_coin`, `member_center_account_info`, `member_center_today_coin_exp`, `member_center_daily_reward`, `member_center_vip_info`, `login_notice`, `login_log`, `login_generate_captcha`, `login_send_qrcode`, `login_check_qrcode_status`, `is_logged_in`, and `is_vip`). Ignored login smoke tests now use `client.login()`. SMS login, logout, signature update, password login, cookie refresh, and other flow-sensitive paths remain excluded.
- Completed non-Probe release cleanup audit batch `shared-core/remaining-flat-api-boundary-audit`: current inventory found 112 public async methods still declared directly inside `impl BpiClient`. They are gated/mutating/session, deprecated dynamic 404 exceptions, Probe-blocked manga download-read, or WBI helper surfaces; no remaining low-risk flat read shim batch was identified.
- Completed non-Probe release cleanup source batch `flat-api/remove-dynamic-deprecated-404-shims`: removed the three deprecated legacy dynamic 404 flat methods (`dynamic_card_detail`, `dynamic_repost_detail`, and `dynamic_spec_item_likes`) plus the now-unused `DynamicCardDetailParams` public param. Dynamic now has only action/publish flat methods remaining.
- Completed non-Probe release cleanup source batch `shared-core/wbi-helper-api-boundary`: removed unused public `get_wbi_sign` and changed `get_wbi_sign2` to crate-visible so WBI signing remains an internal request-construction helper instead of a flat public `BpiClient` API. `utils` now has no remaining public async flat methods.
- Completed non-Probe release policy batch `release/gated-flat-api-policy`: recorded the then-remaining 107 direct flat async methods as compatibility-only under default goal-mode. This policy is now superseded by the later breaking cleanup and write-surface migration: direct `BpiClient` flat async inventory is `COUNT=0`, and old write/session/mutating capability has moved to module clients.
- Completed explicit breaking source batch `flat-api/remove-remaining-legacy-flat-methods`: removed the remaining legacy direct `BpiClient` flat async methods after user approval for breaking API cleanup. Direct public async inventory under `impl BpiClient` is now `COUNT=0`; module clients and shared transport/signing helpers remain.
- Completed and committed explicit non-Probe source batch `write-api/module-client-legacy-write-surface` in `48cd13c feat(api): move legacy write surface to module clients`: old write/session/mutating methods now live on module clients with old method names and signatures, representative RED/GREEN coverage proves the surface exists, direct `impl BpiClient` public async inventory remains `COUNT=0`, and old/new target-file public async inventory is 117/117.

Current:
- Current batch is `release/post-write-surface-doc-sync`. It is a non-Probe release documentation/status sync batch to align the topic docs with `48cd13c`, the `COUNT=0` direct flat state, and `manga/download-read` being marked not implemented. No Probe run, contract promotion, mutating endpoint execution, or `flightdeck/cockpit.md` update is expected.
- Shared-core/domain bridge work has completed `clientinfo/module-client-bridge`, `web_widget/module-client-bridge`, `activity/module-client-bridge`, `audio/module-client-bridge`, `article/module-client-bridge`, `fav/module-client-bridge`, `cheese/module-client-bridge`, `bangumi/module-client-bridge`, `video_ranking/module-client-bridge`, `search/module-client-bridge`, `wallet/module-client-bridge`, `opus/module-client-bridge`, `misc/module-client-bridge`, `message/module-client-bridge`, `manga/module-client-bridge`, `electric/module-client-bridge`, `creativecenter/module-client-bridge`, `dynamic/module-client-bridge`, `vip/module-client-bridge`, `comment/module-client-bridge`, `historytoview/module-client-bridge`, `note/module-client-bridge`, `danmaku/module-client-bridge`, `live/public-core-client-bridge`, `live/remaining-read-client-bridge`, `login/safe-flow-client-bridge`, `user/module-client-bridge`, `video/module-client-bridge`, and `video/collection-player-client-bridge` examples after endpoint contract batches.
- Goal-mode continuation defaults to Probe-backed endpoint contract batches unless a non-Probe bridge batch is explicitly selected and recorded. Do not repeat completed examples such as `video/info-read`, `login/read-info`, or `clientinfo/ip`.
- Remaining normal endpoint work is now about optional idiomatic aliases for module-client write methods, wrapper/static/local helper cleanup, and documentation polish. `manga/download-read` is not implemented in the current migration and should not block those updates. Direct public flat async inventory under `impl BpiClient` is `COUNT=0`; future API work should target module clients or shared internals, not resurrect flat shims.

Verified:
- After commit `6383119`, `cargo fmt --check`, `cargo check --all-features`, and `cargo test --all-features --lib` passed.
- `cargo test --all-features --lib`: 358 passed, 0 failed, 302 ignored.
- For `shared-core/contract-error-label-validation`, `cargo fmt --check`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo check --all-features`, `cargo test --all-features --lib --quiet`, and `git diff --check` passed.
- For `shared-core/payload-request-helpers`, `cargo fmt --check`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo check --all-features`, `cargo test --all-features --lib --quiet`, and `git diff --check` passed.
- For `clientinfo/module-client-bridge`, `cargo fmt --check`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo check --all-features`, `cargo test --all-features --lib --quiet`, and `git diff --check` passed.
- For `electric/private-read-remark-detail-flow`, RED `cargo test --all-features --lib electric --quiet` failed on missing promoted contract/fixture files; after promotion, `cargo test --all-features --lib probe --quiet`, `cargo test --all-features --lib electric --quiet`, `cargo fmt --check`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo check --all-features`, `cargo test --all-features --lib --quiet`, and `git diff --check` passed.
- For `manga/download-read-probe-block`, additional real Probe attempts were run under `target/bpi-probe-runs/manga/download-read/...`; old-shape SDK requests returned API `code = 99`, no contracts were promoted, and real-browser CDP capture proved current reader success requires generated `m2` and `m1` fields. `git diff --check` passed for the tracked notes before the latest status update.
- For `remaining-endpoint-contract-audit`, source/request-call inventory plus promoted contract and status-board review found no new safe read endpoint batch to run. `git diff --check` passed for the tracked docs update; cargo gates were not run because no Rust source or contract files changed.
- For `web_widget/module-client-bridge`, RED `cargo test --all-features --lib web_widget_domain_client_can_be_created --quiet` first failed because `BpiClient::web_widget()` did not exist. After implementation, `cargo test --all-features --lib web_widget --quiet`, `cargo fmt --check`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo check --all-features`, and `cargo test --all-features --lib --quiet` passed.
- For `activity/module-client-bridge`, RED `cargo test --all-features --lib activity_domain_client_can_be_created --quiet` first failed because `BpiClient::activity()` did not exist; RED `cargo test --all-features --lib activity_client --quiet` then failed because the `ActivityClient` endpoint and payload methods did not exist. After implementation, `cargo test --all-features --lib activity --quiet`, `cargo fmt --check`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo check --all-features`, `cargo test --all-features --lib --quiet`, and `git diff --check` passed.
- For `audio/module-client-bridge`, RED `cargo test --all-features --lib audio_domain_client_can_be_created --quiet` first failed because `BpiClient::audio()` did not exist; RED `cargo test --all-features --lib audio::client --quiet` then failed because the `AudioClient` endpoint and payload methods did not exist. After implementation, `cargo test --all-features --lib audio::client --quiet`, `cargo test --all-features --lib audio --quiet`, `cargo fmt --check`, `git diff --check`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo check --all-features`, and `cargo test --all-features --lib --quiet` passed.
- For `article/module-client-bridge`, RED `cargo test --all-features --lib article_domain_client_can_be_created --quiet` first failed because `BpiClient::article()` did not exist; RED `cargo test --all-features --lib article::client --quiet` then failed because the `ArticleClient` endpoint and payload methods did not exist. After implementation, `cargo test --all-features --lib article::client --quiet`, `cargo test --all-features --lib article --quiet`, `cargo fmt --check`, `git diff --check`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo check --all-features`, and `cargo test --all-features --lib --quiet` passed.
- For `fav/module-client-bridge`, RED `cargo test --all-features --lib fav_domain_client_can_be_created --quiet` first failed because `BpiClient::fav()` did not exist; RED `cargo test --all-features --lib fav::client --quiet` then failed because the `FavClient` endpoint and payload methods did not exist. After implementation, `cargo test --all-features --lib fav::client --quiet`, `cargo test --all-features --lib fav --quiet`, `cargo fmt --check`, `git diff --check`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo check --all-features`, and `cargo test --all-features --lib --quiet` passed.
- For `cheese/module-client-bridge`, RED `cargo test --all-features --lib cheese_domain_client_can_be_created --quiet` first failed because `BpiClient::cheese()` did not exist; RED `cargo test --all-features --lib cheese::client --quiet` then failed because the `CheeseClient` endpoint and payload methods did not exist. After implementation, `cargo test --all-features --lib cheese::client --quiet`, `cargo test --all-features --lib cheese --quiet`, `cargo fmt --check`, `git diff --check`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo check --all-features`, and `cargo test --all-features --lib --quiet` passed.
- For `bangumi/module-client-bridge`, RED `cargo test --all-features --lib bangumi_domain_client_can_be_created --quiet` first failed because `BpiClient::bangumi()` did not exist; RED `cargo test --all-features --lib bangumi::client --quiet` then failed because the `BangumiClient` endpoint and payload methods did not exist. After implementation, `cargo test --all-features --lib bangumi::client --quiet`, `cargo test --all-features --lib bangumi --quiet`, `cargo fmt --check`, `git diff --check`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo check --all-features`, and `cargo test --all-features --lib --quiet` passed.
- For `wallet/module-client-bridge`, RED `cargo test --all-features --lib wallet_domain_client_can_be_created --quiet` first failed because `BpiClient::wallet()` did not exist; RED `cargo test --all-features --lib wallet_client --quiet` then failed because the `WalletClient` endpoint and payload methods did not exist. After implementation, `cargo test --all-features --lib wallet --quiet`, `cargo fmt --check`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo check --all-features`, `cargo test --all-features --lib --quiet`, and `git diff --check` passed.
- For `opus/module-client-bridge`, RED `cargo test --all-features --lib opus_domain_client_can_be_created --quiet` first failed because `BpiClient::opus()` did not exist; RED `cargo test --all-features --lib opus_client --quiet` then failed because the `OpusClient` endpoint and payload methods did not exist. After implementation, `cargo test --all-features --lib opus --quiet`, `cargo fmt --check`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo check --all-features`, `cargo test --all-features --lib --quiet`, and `git diff --check` passed.
- For `misc/module-client-bridge`, RED `cargo test --all-features --lib misc_domain_client_can_be_created --quiet` first failed because `BpiClient::misc()` did not exist; RED `cargo test --all-features --lib misc_client --quiet` then failed because the `MiscClient` endpoint and payload methods did not exist. After implementation, `cargo test --all-features --lib misc --quiet`, `cargo fmt --check`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo check --all-features`, `cargo test --all-features --lib --quiet`, and `git diff --check` passed.
- For `message/module-client-bridge`, RED `cargo test --all-features --lib message_domain_client_can_be_created --quiet` first failed because `BpiClient::message()` did not exist; RED `cargo test --all-features --lib message_client --quiet` then failed because the `MessageClient` endpoint and payload methods did not exist. After implementation, `cargo test --all-features --lib message --quiet`, `cargo fmt --check`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo check --all-features`, `cargo test --all-features --lib --quiet`, and `git diff --check` passed.
- For `manga/module-client-bridge`, RED `cargo test --all-features --lib manga_domain_client_can_be_created --quiet` first failed because `BpiClient::manga()` did not exist; RED `cargo test --all-features --lib manga::client --quiet` then failed because the `MangaClient` endpoint and payload methods did not exist. After implementation, `cargo test --all-features --lib manga::client --quiet`, `cargo test --all-features --lib manga --quiet`, `cargo fmt --check`, `git diff --check`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo check --all-features`, and `cargo test --all-features --lib --quiet` passed.
- For `electric/module-client-bridge`, RED `cargo test --all-features --lib electric_domain_client_can_be_created --quiet` first failed because `BpiClient::electric()` did not exist; RED `cargo test --all-features --lib electric::client --quiet` then failed because the `ElectricClient` endpoint and payload methods did not exist. After implementation, `cargo test --all-features --lib electric::client --quiet`, `cargo test --all-features --lib electric --quiet`, `cargo fmt --check`, `git diff --check`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo check --all-features`, and `cargo test --all-features --lib --quiet` passed.
- For `creativecenter/module-client-bridge`, RED `cargo test --all-features --lib creativecenter_domain_client_can_be_created --quiet` first failed because `BpiClient::creativecenter()` did not exist; RED `cargo test --all-features --lib creativecenter::client --quiet` then failed because the `CreativeCenterClient` endpoint and payload methods did not exist. After implementation, `cargo test --all-features --lib creativecenter::client --quiet`, `cargo test --all-features --lib creativecenter --quiet`, `cargo fmt --check`, `git diff --check`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo check --all-features`, and `cargo test --all-features --lib --quiet` passed.
- For `dynamic/module-client-bridge`, RED `cargo test --all-features --lib dynamic_domain_client_can_be_created --quiet` first failed because `BpiClient::dynamic()` did not exist; RED `cargo test --all-features --lib dynamic::client --quiet` then failed because the `DynamicClient` endpoint and payload methods did not exist. After implementation, `cargo test --all-features --lib dynamic::client --quiet`, `cargo test --all-features --lib dynamic --quiet`, `cargo fmt --check`, `git diff --check`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo check --all-features`, and `cargo test --all-features --lib --quiet` passed.
- For `vip/module-client-bridge`, RED `cargo test --all-features --lib vip_domain_client_can_be_created --quiet` first failed because `BpiClient::vip()` did not exist; RED `cargo test --all-features --lib vip_client --quiet` then failed because the `VipClient` endpoint and payload method did not exist. After implementation, `cargo test --all-features --lib vip --quiet`, `cargo fmt --check`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo check --all-features`, `cargo test --all-features --lib --quiet`, and `git diff --check` passed.
- For `comment/module-client-bridge`, RED `cargo test --all-features --lib comment_domain_client_can_be_created --quiet` first failed because `BpiClient::comment()` did not exist; RED `cargo test --all-features --lib comment_client --quiet` then failed because the `CommentClient` endpoint and payload methods did not exist. After implementation, `cargo test --all-features --lib comment::client --quiet`, `cargo test --all-features --lib comment --quiet`, `cargo fmt --check`, `git diff --check`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo check --all-features`, and `cargo test --all-features --lib --quiet` passed.
- For `historytoview/module-client-bridge`, RED `cargo test --all-features --lib historytoview_domain_client_can_be_created --quiet` first failed because `BpiClient::historytoview()` did not exist; RED `cargo test --all-features --lib historytoview::client --quiet` then failed because the `HistoryToViewClient` endpoint and payload methods did not exist. After implementation, `cargo test --all-features --lib historytoview::client --quiet`, `cargo test --all-features --lib historytoview --quiet`, `cargo fmt --check`, `git diff --check`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo check --all-features`, and `cargo test --all-features --lib --quiet` passed.
- For `note/module-client-bridge`, RED `cargo test --all-features --lib note_domain_client_can_be_created --quiet` first failed because `BpiClient::note()` did not exist; RED `cargo test --all-features --lib note::client --quiet` then failed because the `NoteClient` endpoint and payload methods did not exist. After implementation, `cargo test --all-features --lib note::client --quiet`, `cargo test --all-features --lib note --quiet`, `cargo fmt --check`, `git diff --check`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo check --all-features`, and `cargo test --all-features --lib --quiet` passed.
- For `video_ranking/module-client-bridge`, RED `cargo test --all-features --lib video_ranking_domain_client_can_be_created --quiet` first failed because `BpiClient::video_ranking()` did not exist; RED `cargo test --all-features --lib video_ranking::client --quiet` then failed because the `VideoRankingClient` endpoint and payload methods did not exist. After implementation, `cargo test --all-features --lib video_ranking::client --quiet`, `cargo test --all-features --lib video_ranking --quiet`, `cargo fmt --check`, `git diff --check`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo check --all-features`, and `cargo test --all-features --lib --quiet` passed.
- For `search/module-client-bridge`, RED `cargo test --all-features --lib search_domain_client_can_be_created --quiet` first failed because `BpiClient::search()` did not exist; RED `cargo test --all-features --lib search::client --quiet` then failed because the `SearchClient` endpoint and payload methods did not exist. After implementation, `cargo test --all-features --lib search::client --quiet`, `cargo test --all-features --lib search --quiet`, `cargo fmt --check`, `git diff --check`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo check --all-features`, and `cargo test --all-features --lib --quiet` passed.
- For `danmaku/module-client-bridge`, RED `cargo test --all-features --lib danmaku_domain_client_can_be_created --quiet` first failed because `BpiClient::danmaku()` did not exist; RED `cargo test --all-features --lib danmaku::client --quiet` then failed because the `DanmakuClient` endpoint and read methods did not exist. After implementation, `cargo test --all-features --lib danmaku::client --quiet`, `cargo test --all-features --lib danmaku --quiet`, `cargo fmt --check`, `git diff --check`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo check --all-features`, and `cargo test --all-features --lib --quiet` passed.
- For `live/public-core-client-bridge`, RED `cargo test --all-features --lib live::client --quiet` first failed because `LiveClient` and `BpiClient::live()` did not exist. After implementation, `cargo test --all-features --lib live::client --quiet`, `cargo test --all-features --lib live --quiet`, `cargo fmt --check`, `git diff --check`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo check --all-features`, and `cargo test --all-features --lib --quiet` passed.
- For `login/safe-flow-client-bridge`, RED `cargo test --all-features --lib login::client --quiet` first failed because `LoginClient` lacked notice, log, captcha, QR generate, QR poll, and endpoint accessor methods. After implementation, `cargo test --all-features --lib login::client --quiet`, `cargo test --all-features --lib login --quiet`, `cargo fmt --check`, `git diff --check`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo check --all-features`, and `cargo test --all-features --lib --quiet` passed.
- For `user/module-client-bridge`, RED `cargo test --all-features --lib user::client::tests::user_client_methods_use_payload_request_helpers --quiet` first failed because `UserClient` still used legacy envelope request helpers. After implementation, `cargo test --all-features --lib user::client::tests::user_client_methods_use_payload_request_helpers --quiet`, `cargo test --all-features --lib user --quiet`, `cargo test --all-features --lib user_domain_client_can_be_created --quiet`, `cargo fmt --check`, `git diff --check`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo check --all-features`, and `cargo test --all-features --lib --quiet` passed.
- For `video/module-client-bridge`, RED `cargo test --all-features --lib video::client::tests::video_client_methods_use_payload_request_helpers --quiet` first failed because `VideoClient` still used legacy envelope helpers and the flat `video_playurl` bridge. After implementation, `cargo test --all-features --lib video::client::tests::video_client_methods_use_payload_request_helpers --quiet`, `cargo test --all-features --lib video::client --quiet`, `cargo test --all-features --lib video::videostream_url --quiet`, `cargo test --all-features --lib video --quiet`, `cargo fmt --check`, `git diff --check`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo check --all-features`, and `cargo test --all-features --lib --quiet` passed.
- For `video/collection-player-client-bridge`, RED `cargo test --all-features --lib video::client::tests::video_client_exposes_collection_and_player_read_methods --quiet` first failed because `VideoClient` lacked collection/player read methods. After implementation, `cargo test --all-features --lib video::client::tests::video_client_exposes_collection_and_player_read_methods --quiet`, `cargo test --all-features --lib video::client --quiet`, `cargo test --all-features --lib video::collection --quiet`, `cargo test --all-features --lib video::online --quiet`, `cargo test --all-features --lib video::player --quiet`, `cargo test --all-features --lib video::recommend --quiet`, `cargo test --all-features --lib video::summary --quiet`, `cargo test --all-features --lib video::tags --quiet`, `cargo test --all-features --lib video::interact_video --quiet`, `cargo test --all-features --lib video --quiet`, `cargo fmt --check`, `git diff --check`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo check --all-features`, and `cargo test --all-features --lib --quiet` passed.
- For `live/remaining-read-client-bridge`, RED `cargo test --all-features --lib live::client::tests::live_client_exposes_remaining_read_methods --quiet` first failed because `LiveClient` lacked the 15 remaining promoted read methods. After implementation, `cargo test --all-features --lib live::client::tests::live_client_exposes_remaining_read_methods --quiet`, `cargo test --all-features --lib live::client --quiet`, `cargo test --all-features --lib live --quiet`, `cargo fmt --check`, `git diff --check`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo check --all-features`, and `cargo test --all-features --lib --quiet` passed. Full lib tests reported 929 passed, 0 failed, 292 ignored.
- For `shared-core/module-client-coverage-audit`, contract/client inventory found 206 promoted contracts and only one contract name absent from `src/**/client.rs`: `login.qr.flow`. Review showed it is a Probe flow contract over existing `qr_generate` and `qr_poll` methods, not a missing endpoint bridge. `git diff --check` passed; cargo gates were not run because no Rust source, tests, or contract files changed.
- For `docs/readme-module-api-refresh`, `git diff --check` passed with only CRLF conversion warnings. README stale-API searches for `BpiClient::new();`, `.data.unwrap()`, `login_send_qrcode`, `login_check_qrcode_status`, `bangumi_info(`, the old account-loading log text, and old "统一结构体" wording returned no matches. Cargo gates were not run because only README and topic docs changed.
- For `docs/migration-guide-0.2`, `git diff --check` passed with only CRLF conversion warnings. Stale-API searches across README and `docs/migration-0.2.md` for `BpiClient::new();`, `.data.unwrap()`, `login_send_qrcode`, `login_check_qrcode_status`, and `bangumi_info(` returned no matches. Taskfile audit found `test_offline` and `check_all` present and no `blackroom`, `emoji`, or `garb` stale tasks. Cargo gates were not run because only docs and topic files changed.
- For `examples/module-client-quickstart`, `cargo fmt --check`, `cargo check --all-features --examples`, and `git diff --check` passed. Stale-API search across README, `docs/migration-0.2.md`, and `examples/module_clients.rs` returned no matches. `git diff -- flightdeck/cockpit.md` showed no diff. Full library gates were not run because only a compiled example plus docs/topic metadata changed.
- For `examples/stale-binary-quickstart-refresh`, `cargo fmt --check`, `cargo check --all-features --bins`, and `git diff --check` passed. Stale executable-doc search across `src/main.rs`, README, `docs/migration-0.2.md`, and `examples/module_clients.rs` returned no matches for old flat quickstart APIs. `git diff -- flightdeck/cockpit.md` showed no diff. Full library gates were not run because no library module, contract, or test behavior changed.
- For `release/rustdoc-example-audit`, `cargo test --doc`, `cargo fmt --check`, and `git diff --check` passed. Rustdoc reported 0 doc tests, so no source-doc edits were needed. `git diff -- flightdeck/cockpit.md` showed no diff.
- For `release/current-stack-full-gate`, `task check_all` passed, covering `cargo fmt --check`, strict clippy, `cargo check --all-features`, and `cargo test --all-features --lib`; lib tests reported 929 passed, 0 failed, 292 ignored. `cargo check --all-features --examples`, `cargo check --all-features --bins`, `cargo test --doc`, and `git diff --check` also passed. `git diff -- flightdeck/cockpit.md` showed no diff, stale API searches returned no matches, and ignored-artifact status confirmed `migration-status.md` plus `target/` remain ignored.
- For `shared-core/legacy-flat-api-surface-audit`, `git diff --check` passed with only CRLF conversion warnings. `git diff -- flightdeck/cockpit.md` showed no diff. Ignored-artifact status confirmed `migration-status.md` plus `target/` remain ignored. Cargo gates were not run because only an audit note and topic metadata changed.
- For `flat-api/remove-small-read-shims`, stale flat API search returned no matches across `src`, `tests`, `examples`, `README.md`, and `docs`. Focused tests passed for `clientinfo`, `wallet`, `opus`, and `web_widget`. `cargo fmt --check`, `cargo check --all-features`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo test --all-features --lib --quiet`, `git diff --check`, and `git diff -- flightdeck/cockpit.md` passed; full lib tests reported 929 passed, 0 failed, 292 ignored.
- For `flat-api/remove-activity-message-read-shims`, stale flat API search returned no matches across `src`, `tests`, `examples`, `README.md`, and `docs`. Focused tests passed for `activity` and `message`. `cargo fmt --check`, `cargo check --all-features`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo test --all-features --lib --quiet`, `git diff --check`, and `git diff -- flightdeck/cockpit.md` passed; full lib tests reported 929 passed, 0 failed, 292 ignored.
- For `flat-api/remove-bangumi-cheese-read-shims`, stale flat API search returned no matches across `src`, `tests`, `examples`, `README.md`, and `docs`. Focused tests passed for `bangumi` and `cheese`. `cargo fmt --check`, `cargo check --all-features`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo test --all-features --lib --quiet`, `git diff --check`, and `git diff -- flightdeck/cockpit.md` passed; full lib tests reported 929 passed, 0 failed, 292 ignored.
- For `flat-api/remove-search-video-ranking-read-shims`, stale flat API search returned no matches across `src`, `tests`, `examples`, `README.md`, and `docs`. Focused tests passed for `search` and `video_ranking`. `cargo fmt --check`, `cargo check --all-features`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo test --all-features --lib --quiet`, `git diff --check`, and `git diff -- flightdeck/cockpit.md` passed; full lib tests reported 929 passed, 0 failed, 292 ignored.
- For `flat-api/remove-fav-note-history-comment-read-shims`, stale flat definition and direct old-call searches returned no matches across `src`, `tests`, `examples`, `README.md`, and `docs`. Focused tests passed for `fav`, `note`, `historytoview`, and `comment`. `cargo fmt --check`, `cargo check --all-features`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo test --all-features --lib --quiet`, `git diff --check`, and `git diff -- flightdeck/cockpit.md` passed; full lib tests reported 929 passed, 0 failed, 292 ignored.
- For `flat-api/remove-misc-vip-read-shims`, stale flat definition and direct old-call searches returned no matches across `src`, `tests`, `examples`, `README.md`, and `docs`. Focused tests passed for `misc` and `vip`. `cargo fmt --check`, `cargo check --all-features`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo test --all-features --lib --quiet`, `git diff --check`, and `git diff -- flightdeck/cockpit.md` passed; full lib tests reported 929 passed, 0 failed, 292 ignored.
- For `flat-api/remove-article-manga-read-shims`, stale flat definition and direct old-call searches returned no matches across `src`, `tests`, `examples`, `README.md`, and `docs`. Focused tests passed for `article` and `manga`. `cargo fmt --check`, `cargo check --all-features`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo test --all-features --lib --quiet`, `git diff --check`, and `git diff -- flightdeck/cockpit.md` passed; full lib tests reported 929 passed, 0 failed, 292 ignored.
- For `flat-api/remove-electric-read-shims`, stale flat definition and direct old-call searches returned no matches across `src`, `tests`, `examples`, `README.md`, and `docs`. Focused tests passed for `electric`. `cargo fmt --check`, `cargo check --all-features`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo test --all-features --lib --quiet`, `git diff --check`, and `git diff -- flightdeck/cockpit.md` passed; full lib tests reported 929 passed, 0 failed, 292 ignored.
- For `flat-api/remove-audio-read-shims`, stale flat definition and direct old-call searches returned no matches across `src`, `tests`, `examples`, `README.md`, and `docs`. Focused tests passed for `audio`. `cargo fmt --check`, `cargo check --all-features`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo test --all-features --lib --quiet`, `git diff --check`, and `git diff -- flightdeck/cockpit.md` passed; full lib tests reported 929 passed, 0 failed, 292 ignored.
- For `flat-api/remove-video-read-shims`, stale flat definition and direct old-call searches returned no matches across `src`, `tests`, `examples`, `README.md`, and `docs`. Focused tests passed for `video`. `cargo fmt --check`, `cargo check --all-features`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo test --all-features --lib --quiet`, `git diff --check`, and `git diff -- flightdeck/cockpit.md` passed; full lib tests reported 929 passed, 0 failed, 292 ignored.
- For `flat-api/remove-creativecenter-read-shims`, stale direct flat-call searches returned no matches across `src`, `tests`, `examples`, `README.md`, and `docs`. Focused tests passed for `creativecenter`. `cargo fmt --check`, `cargo check --all-features`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo test --all-features --lib --quiet`, `git diff --check`, and `git diff -- flightdeck/cockpit.md` passed; full lib tests reported 929 passed, 0 failed, 292 ignored.
- For `flat-api/remove-danmaku-read-shims`, stale direct flat-call searches returned no matches across `src`, `tests`, `examples`, `README.md`, and `docs`. Focused tests passed for `danmaku`. `cargo fmt --check`, `cargo check --all-features`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo test --all-features --lib --quiet`, `git diff --check`, and `git diff -- flightdeck/cockpit.md` passed; full lib tests reported 929 passed, 0 failed, 292 ignored.
- For `flat-api/remove-dynamic-read-shims`, stale direct flat-call searches returned no matches across `src`, `tests`, `examples`, `README.md`, and `docs`; remaining `pub async fn dynamic_*` definitions are only the excluded deprecated/action/publish methods. Focused tests passed for `dynamic`. `cargo fmt --check`, `cargo check --all-features`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo test --all-features --lib --quiet`, `git diff --check`, and `git diff -- flightdeck/cockpit.md` passed; full lib tests reported 929 passed, 0 failed, 292 ignored.
- For `flat-api/remove-user-read-shims`, stale direct flat-call searches returned no matches across `src`, `tests`, `examples`, `README.md`, and `docs`; remaining `impl BpiClient` user definitions are only excluded relation group/action and space-notice mutations. Focused tests passed for `user`. `cargo fmt --check`, `cargo check --all-features`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo test --all-features --lib --quiet`, `git diff --check`, and `git diff -- flightdeck/cockpit.md` passed; full lib tests reported 929 passed, 0 failed, 292 ignored.
- For `flat-api/remove-live-read-shims`, stale direct flat-call searches returned no matches across `src`, `tests`, `examples`, `README.md`, and `docs`; selected `pub async fn live_*` definition scan returned no matches in `src/live`. Focused tests passed for `live` with 62 passed and 35 ignored. `cargo fmt --check`, `cargo check --all-features`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo test --all-features --lib --quiet`, `git diff --check`, and `git diff -- flightdeck/cockpit.md` passed; full lib tests reported 929 passed, 0 failed, 292 ignored.
- For `flat-api/remove-login-safe-read-flow-shims`, stale direct flat-call searches returned no matches across `src`, `tests`, `examples`, `README.md`, and `docs`; selected `pub async fn` definition scan returned no matches in `src/login`. Focused tests passed for `login` with 77 passed and 11 ignored. `cargo fmt --check`, `cargo check --all-features`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo test --all-features --lib --quiet`, `git diff --check`, and `git diff -- flightdeck/cockpit.md` passed; full lib tests reported 929 passed, 0 failed, 292 ignored.
- For `shared-core/remaining-flat-api-boundary-audit`, current `impl BpiClient` inventory found 112 remaining direct flat async methods across 19 modules. The audit classifies them as gated/mutating/session, deprecated/documented exception, Probe-blocked read, or helper/API decision surfaces. Evidence grep passed; `git diff --check` passed with only CRLF conversion warnings; `git diff -- flightdeck/cockpit.md` returned no diff; ignored status check showed `migration-status.md` and `target/` remain ignored. Cargo gates were not run because this audit changed only Flightdeck planning docs.
- For `flat-api/remove-dynamic-deprecated-404-shims`, stale direct flat-call searches returned no matches across `src`, `tests`, `examples`, `README.md`, and `docs`; `DynamicCardDetailParams` search returned no matches. Focused tests passed for `dynamic` with 48 passed and 23 ignored. `cargo fmt --check`, `cargo check --all-features`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, `cargo test --all-features --lib --quiet`, `git diff --check`, and `git diff -- flightdeck/cockpit.md` passed; full lib tests reported 924 passed, 0 failed, 292 ignored. Current remaining direct flat async inventory is 109 total, and `dynamic` has 7 remaining methods, all action/publish write surfaces.
- For `shared-core/wbi-helper-api-boundary`, public WBI helper searches returned no matches across `src`, README, docs, and examples. Focused `utils::wbi` tests passed with 1 ignored legacy live test. `cargo fmt --check`, `cargo check --all-features`, `cargo clippy --all-targets --all-features --locked -- -D warnings`, and `cargo test --all-features --lib --quiet` passed; full lib tests reported 924 passed, 0 failed, 291 ignored. Current remaining direct flat async inventory is 107 total, and `utils` has no remaining public async flat methods.
- For `release/gated-flat-api-policy`, policy evidence search found the batch marker, `COUNT=107` inventory, `BPI_MUTATING_TEST` gate, `manga/download-read` block, compatibility-only policy, and No default source cleanup language across the policy doc, batch todo, topic index, and local status board. `git diff --check` exited 0 with only existing LF/CRLF conversion warnings; `git diff -- flightdeck/cockpit.md` showed no diff; ignored-artifact status confirmed `migration-status.md` and `target/` remain ignored. Cargo gates were not run because this batch changed only Flightdeck planning docs and local status.
- For `write-api/module-client-legacy-write-surface`, RED `cargo test --all-features --lib module_clients_expose_legacy_write_capability_futures --quiet` first failed because representative old write methods were absent from module clients. After implementation, the focused test passed; `cargo check --all-features`, strict clippy, `cargo fmt --check`, full lib tests, examples check, bins check, doc tests, `git diff --check`, direct `BpiClient` public async inventory, and old/new target-file public async inventory checks passed. Full lib tests reported 915 passed, 0 failed, 196 ignored.

## Local-only Constraints

- Do not commit `migration-status.md`.
- Do not commit raw Probe outputs, account-specific response data, cookies, `SESSDATA`, `bili_jct`, `buvid`, or local account notes.
- Keep raw Probe output under `target/bpi-probe-runs/...`.
- Keep request drafts under `target/bpi-contract-drafts/...`.
- Commit only reviewed endpoint contracts and sanitized response fixtures under `tests/contracts/...`.
- Prefer one commit per completed module batch; do not create endpoint-sized commit spam.

## Open questions

- None blocking the design review.
